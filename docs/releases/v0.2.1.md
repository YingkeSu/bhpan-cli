# bhpan-cli v0.2.1 Release Notes

发布日期：2026-03-18

## 修复

- 修复 `rm -r /path`、`head -n 5 file`、`tail -n 5 file`、`mv -f src dst`、`cp -f src dst` 的参数顺序问题
- 修复 `npm test` 无法运行的问题，并将测试重新纳入发布前检查
- 修复 `ls -R` 不显示目标目录本身的问题，使 `-L 0` 和针对目标路径的 `--regex` 过滤按预期工作
- 修复 README 当前版本号与代码块渲染错误
